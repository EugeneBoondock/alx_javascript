function printStrings(strings) {
  for (let i = 0; i < strings.length; i++) {
    console.log(strings[i].trim());
  }
}

const stringsArray = ["C is fun\n", "Python is cool\n", "JavaScript is amazing"];
printStrings(stringsArray);
